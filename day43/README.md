Ex_day43_week9

Python – Sum of Even Numbers Function Write a function that receives a list and returns the sum of all even numbers.

NumPy – 3x3 Matrix Operations Generate a random 3×3 matrix and compute its determinant, transpose, and inverse.

Pandas – Data Cleaning Task Create a DataFrame, insert NaN values, clean them using mean, and calculate salary averages for age > 30.

Matplotlib – Quadratic Function Plot Plot the function y = x^2 − 5x + 6 over the range [-10, 10] with grid enabled.

Machine Learning – Decision Tree Classifier Build a synthetic dataset, split into train/test, train a Decision Tree classifier, print accuracy, and test a new input.
