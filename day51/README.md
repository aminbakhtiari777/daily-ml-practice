This notebook builds a simple neural network (MLP) for the MNIST dataset using TensorFlow/Keras.
It includes image normalization, one-hot encoding of labels, and training a Dense model with Softmax output.
The model learns to classify handwritten digits and is evaluated on test accuracy.
