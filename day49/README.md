NumPy → Generate random matrix and apply Z-score normalization

Pandas → Create new engineered features and analyze correlation with salary

Linear Regression → Predict salary and interpret coefficients

Logistic Regression → Classify high_income and analyze errors

Random Forest → Compare non-linear model with linear model and inspect feature importance
